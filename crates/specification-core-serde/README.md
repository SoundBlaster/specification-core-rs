# specification-core-serde

Optional, declarative serialization support for `specification-core` rule
documents. The schema is versioned independently from the crate version and
does not attempt to serialize arbitrary Rust closures or trait objects.
